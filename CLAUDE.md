# CLAUDE.md — 开发设计思路

本项目（`emailAssistant-obsidian`）的配置体系遵循**三角色模型**。所有配置相关的新增和修改都必须遵守它。

## 三角色定义

```
.env（实现者）          config.py（定义者）           业务代码（调用者）
┌────────────────┐      ┌──────────────────────┐     ┌──────────────────┐
│ 显式给出每个值   │ ───> │ os.environ[KEY]      │ ──> │ import config     │
│ 不留任何悬念     │      │ 必填读取，零默认值    │     │ 只消费 config 字段 │
└────────────────┘      └──────────────────────┘     └──────────────────┘
```

- **`.env` 是实现者** —— 每个配置项都显式写值。不写「注释掉的默认值」，不依赖代码里的回退。改配置只动 `.env`，不碰代码。
- **`config.py` 是定义者** —— 只做三件事：
  1. `os.environ[KEY]` 必填报错式读取（缺配置立刻 KeyError，绝不静默回退）；
  2. 定义跨模块共享的键名和派生逻辑（如逗号分隔列表解析）；
  3. 必要的环境预处理（如把网关地址注入 LiteLLM 的 `OPENAI_API_BASE`）。
  - **禁止任何形式的默认值**：`os.environ.get(KEY, "默认")` 不允许，即使默认值看起来是「常识」（如 `.trash`、`.obsidian`、`http://localhost:6333`）。可选配置项（如 `RESPONSE_TEMPERATURE`、`HTTPS_PROXY`）可以用 `os.environ.get(KEY)` 无默认读取，语义就是「未设置则代码里走 None 分支」。
- **业务代码是调用者** —— `import config` 后只消费 `config.xxx` 字段，不直接读 `os.environ`，不自带兜底默认值。

## 为什么这样设计

- 改配置时只动 `.env`，不用碰代码——换模型、换网关、换排除目录都是纯配置操作。
- 缺配置会在启动瞬间炸出 KeyError，而不是带默认值跑到一半才暴露行为异常。
- `.env.example` 与 `.env` 一一对应，新机器部署时照抄一遍即可。

## 同步规则

- 新增配置项时三个文件一起改：`.env`（真实值）、`.env.example`（示例值 + 中文注释）、`config.py`（定义）。
- 排除项、过滤列表（如 `OBSIDIAN_EXCLUDE_FOLDERS`）一律走这个流程，不允许在业务逻辑里硬编码。
